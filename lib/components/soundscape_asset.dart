import 'package:flutter/material.dart';

class SoundscapeAsset {
  final String title;
  final String soundPath;
  final String imagePath;

  SoundscapeAsset(
      {@required this.soundPath,
      @required this.imagePath,
      @required this.title});
}
